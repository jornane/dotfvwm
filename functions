# [I]mmediate, [C]lick, [D]oubleclick, click-and-[M]ove

DestroyFunc StartFunction
AddToFunc StartFunction
+ I Test (Init) Exec xinput set-prop "SynPS/2 Synaptics TouchPad" "Device Enabled" 0
+ I Test (Init) Exec xinput set-prop "TPPS/2 IBM TrackPoint" "Evdev Wheel Emulation" 0
+ I Test (Init) Exec xinput set-prop "TPPS/2 IBM TrackPoint" "Evdev Wheel Emulation Button" 0
+ I Test (Init) Exec xinput set-prop "TPPS/2 IBM TrackPoint" "Evdev Middle Button Emulation" 0
+ I Test (Init) Exec xinput set-prop "TPPS/2 IBM TrackPoint" "Evdev Middle Button Timeout" 0
+ I Test (Init) Exec xsetroot -solid '#356390'
+ I Test (Init) Exec xset b 0
+ I Test (Init) Exec xrdb -merge $[FVWM_USERDIR]/Xresources
+ I Test (Init) Exec xmodmap $[FVWM_USERDIR]/Xmodmap
+ I Test (Init) Exec xclock -digital -brief -twentyfour -padding 3 -geometry 48x24-0+0 -bg black -fg white
+ I Test (Init) Exec xload -nolabel -jumpscroll 1 -hl grey -geometry 48x24-49+0 -rv
+ I Test (Init) Exec xscreensaver -no-splash
+ I Test (Init) Exec ssh-add $(for i in ~/.ssh/id*pub ; do echo ${i%.pub} ; done)
+ I Test (Init) Exec stalonetray -bg "#356390" --geometry 1x1-102+0 --window-strut none & nm-applet
+ I Module FvwmPager 0 1

DestroyFunc ExitFunction
AddToFunc ExitFunction

DestroyFunc Raise-and-move
AddToFunc Raise-and-move
+ I Raise
+ M Move
+ D Maximize 0 100

DestroyFunc Raise-and-resize
AddToFunc Raise-and-resize
+ I Raise
+ M Resize
+ D RaiseLower

DestroyFunc Winlist
AddToFunc Winlist
+ I WindowList Alphabetic, NoGeometryWithInfo, NoDeskNum, NoHotkeys, NoCurrentDeskTitle

DestroyFunc Menu16
AddToFunc Menu16
+ I PipeRead 'rm $[FVWM_USERDIR]/menusize 1>&2'
+ I Read menus

DestroyFunc Menu32
AddToFunc Menu32
+ I PipeRead 'ln -s $[FVWM_USERDIR]/system/menusize $[FVWM_USERDIR]/menusize 1>&2'
+ I Read menus
